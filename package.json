{
  "name": "smartkode",
  "version": "1.0.0",
  "description": "Electron app for speech-to-text, PSOAP note generation, and ICPC-2 coding",
  "main": "electron-main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . smartkode --platform=win32 --arch=x64 --out=release --overwrite"
  },
  "dependencies": {
    "electron": "^25.0.0",
    "sqlite3": "^5.1.7",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "electron-packager": "^17.1.2"
  }
}
